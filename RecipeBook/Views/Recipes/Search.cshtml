@{
  Layout = "_Layout";
}

@using RecipeBook.Models;

@if (@Model.Count == 0)
{
  <h3>This search found no results</h3>
}

<ul>
  @foreach (Recipe recipe in Model)
  {
    <li class="lists">@Html.ActionLink(@recipe.Name, "Details", new { id = @recipe.RecipeId })</li>
  }
</ul>